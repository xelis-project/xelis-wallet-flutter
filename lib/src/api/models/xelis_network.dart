/// XELIS network selected for a wallet.
///
/// This authored type keeps generated Flutter Rust Bridge models out of the
/// supported public API.
enum XelisNetwork { mainnet, testnet, devnet, stagenet }
