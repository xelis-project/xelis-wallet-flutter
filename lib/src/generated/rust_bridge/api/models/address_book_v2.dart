// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.13.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../../frb_generated.dart';

import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;
part 'address_book_v2.freezed.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`

/// One destination-first address-book record.
@freezed
sealed class NativeAddressBookEntry with _$NativeAddressBookEntry {
  const factory NativeAddressBookEntry({
    required String id,
    required String displayName,
    String? destinationLabel,
    String? note,
    required NativeSavedDestination destination,
  }) = _NativeAddressBookEntry;
}

/// Result of matching a standard or integrated address against saved entries.
///
/// `Exact` and `BaseOnly` contain one entry, `Ambiguous` contains at least two
/// entries sharing a base address, and `None` contains no entry.
@freezed
sealed class NativeAddressBookMatch with _$NativeAddressBookMatch {
  const factory NativeAddressBookMatch({
    required NativeAddressBookMatchKind kind,
    required List<NativeAddressBookEntry> entries,
  }) = _NativeAddressBookMatch;
}

enum NativeAddressBookMatchKind { exact, baseOnly, ambiguous, none }

@freezed
sealed class NativeAddressBookMigrationResult
    with _$NativeAddressBookMigrationResult {
  const factory NativeAddressBookMigrationResult({
    required int migratedEntries,
    required bool alreadyComplete,
  }) = _NativeAddressBookMigrationResult;
}

@freezed
sealed class NativeAddressBookPage with _$NativeAddressBookPage {
  const factory NativeAddressBookPage({
    required List<NativeAddressBookEntry> entries,
    required int total,
    required bool hasMore,
  }) = _NativeAddressBookPage;
}

/// Shape of the data embedded in an integrated address.
///
/// The value itself deliberately never crosses this bridge contract. This is
/// enough for UI labeling and diagnostics without duplicating or exposing the
/// protocol payload stored in the canonical address.
enum NativeIntegratedDataKind {
  boolValue,
  string,
  u8,
  u16,
  u32,
  u64,
  u128,
  hash,
  blob,
  array,
  fields,
}

/// Canonical destination owned by one address-book entry.
@freezed
sealed class NativeSavedDestination with _$NativeSavedDestination {
  const factory NativeSavedDestination({
    required String address,
    required String baseAddress,
    required NativeSavedDestinationKind kind,
    NativeIntegratedDataKind? integratedDataKind,
  }) = _NativeSavedDestination;
}

/// Package-owned classification of a stored XELIS destination.
enum NativeSavedDestinationKind { standard, integrated }
